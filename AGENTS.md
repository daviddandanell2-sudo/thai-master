# Agent Instructions

This repository controls the Private Tutoring Thailand system.

Always read README.md before doing any work.

Work only inside this repository.

Do not create files outside this folder.

Do not build the full website yet.

Do not create hundreds of SEO pages yet.

First create the structure, documentation files, data files, templates, and prompt files.

Follow the repository structure defined in README.md.

Use markdown for documentation and content.

Use JSON only inside /data.

Every page must have a clear SEO purpose, parent intent, internal links, and conversion action.

Do not invent fake numbers, fake testimonials, fake tutor credentials, fake school partnerships, or fake claims.

Do not duplicate pages by only changing city names.

Before editing files, show the plan.

After editing files, show what changed.

After each major step, run git status.

When unsure, ask before changing the structure.
